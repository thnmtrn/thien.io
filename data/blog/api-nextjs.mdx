---
title: 'APIs with NextJS'
publishedAt: '2021-02-28'
summary: 'how to get data with nextjs from various db'
image: '/static/images/backend/banner.jpg'
---

```js
export async function getStaticProps(context) {
  return {
    props: {}
  };
}
```

then we add try catch

```js
export async function getStaticProps(context) {
  try {
    const res = await fetch('');
    const data = await res.json();
  } catch (err) {
    console.error(err);
  }
  return {
    props: {}
  };
}
```
