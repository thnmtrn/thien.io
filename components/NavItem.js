import { useState } from 'react'

function NavItem(props) {
    const [open, setOpen] = useState(false)
    return (
        <li className="nav-item flex items-center justify-items-center">
            a
        </li>
    )
}

export default NavItem
